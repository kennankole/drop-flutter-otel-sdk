import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Replaced by real coverage once L1 lands the public API surface
    // (lib/drop_observability.dart currently exports nothing to test).
    expect(1 + 1, 2);
  });
}
