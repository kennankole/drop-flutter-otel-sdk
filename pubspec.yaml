name: drop_observability
description: >
  Shared Flutter OTEL instrumentation package for drop-mobile, drop-rider,
  and drop-admin-mobile. See OTEL_LIBRARY_PLAN.md (in drop-mobile) for the
  design and build phases.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.10.4
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
